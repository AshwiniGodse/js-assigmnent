/*accept the input of name from the
 user and print it in the console. */

let name = prompt('What is your name?');
console.log(name);

/* Accept the input of age from
the uer and print the output in the console*/ 
let age = prompt('What is your age?');
console.log(age);